<Window x:Class="dashbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:dashbot"
        mc:Ignorable="d"
        Title="YGO Bot Launcher v2.3" Height="700" Width="1040"
        Background="#F4F6F9" TextElement.Foreground="#111111"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="12"
        Icon="dashbot.ico">
    <Window.Resources>
        <!-- Modern Premium Controls Styling -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="BorderBrush" Value="#222222"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="5,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="BorderBrush" Value="#222222"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="5,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Premium Charcoal Dark Button -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#222222"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                    <Setter Property="BorderBrush" Value="#444444"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#111111"/>
                    <Setter Property="BorderBrush" Value="#111111"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#888888"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header (AI clean-up) -->
        <Border Grid.Row="0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1.5" Padding="5,0,5,12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="YGO Bot Launcher v2.3" FontSize="22" FontWeight="Bold" Foreground="#111111"/>
                    <TextBlock Text="Standalone client connecting WindBot to EDOPro room" FontSize="11" Foreground="#666666" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="WindBot Engine" FontSize="11" Foreground="#666666" Margin="0,0,15,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TxtTime" Text="00:00:00" VerticalAlignment="Center" FontSize="14" Foreground="#111111" FontFamily="Consolas" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Workspace -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Launcher Controls (Left Column) -->
            <GroupBox Grid.Column="0" Header="Launcher Connection" Margin="0,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Background="Transparent">
                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Target Host IP:" Margin="0,0,0,4" FontWeight="Bold" Foreground="#111111"/>
                        <TextBox x:Name="TxtHostIp" Text="127.0.0.1" Margin="0,0,0,12"/>

                        <TextBlock Text="Host Port:" Margin="0,0,0,4" FontWeight="Bold" Foreground="#111111"/>
                        <TextBox x:Name="TxtHostPort" Text="7911" Margin="0,0,0,12"/>

                        <TextBlock Text="Select Deck Profile:" Margin="0,0,0,6" FontWeight="Bold" Foreground="#111111"/>
                        <RadioButton x:Name="RbLegacy" Content="Windbot Legacy Decks" GroupName="DeckGroup" IsChecked="True" Checked="RbCategory_Checked"/>
                        <ComboBox x:Name="CbDecksLegacy" Height="28" Margin="15,0,0,10" SelectionChanged="CbDecksLegacy_SelectionChanged"/>
                        
                        <RadioButton x:Name="Rb2026" Content="New 2026 Decks" GroupName="DeckGroup" Checked="RbCategory_Checked"/>
                        <ComboBox x:Name="CbDecks2026" Height="28" Margin="15,0,0,10" SelectionChanged="CbDecks2026_SelectionChanged"/>
                        
                        <RadioButton x:Name="RbOther" Content="Other Decks" GroupName="DeckGroup" Checked="RbCategory_Checked"/>
                        <ComboBox x:Name="CbDecksOther" Height="28" Margin="15,0,0,15" SelectionChanged="CbDecksOther_SelectionChanged"/>

                        <CheckBox x:Name="ChkTwoBots" Content="Spawn 2 Bots (Bot vs Bot)" Margin="0,5,0,12" Click="ChkTwoBots_Click"/>

                        <StackPanel x:Name="OpponentDeckPanel" Visibility="Collapsed" Margin="0,0,0,15">
                            <TextBlock Text="Select Opponent Deck Profile:" Margin="0,0,0,6" FontWeight="Bold" Foreground="#111111"/>
                            <RadioButton x:Name="RbOpponentLegacy" Content="Windbot Legacy Decks" GroupName="OpponentDeckGroup" IsChecked="True" Checked="RbOpponentCategory_Checked"/>
                            <ComboBox x:Name="CbOpponentDecksLegacy" Height="28" Margin="15,0,0,10" SelectionChanged="CbOpponentDecksLegacy_SelectionChanged"/>
                            
                            <RadioButton x:Name="RbOpponent2026" Content="New 2026 Decks" GroupName="OpponentDeckGroup" Checked="RbOpponentCategory_Checked"/>
                            <ComboBox x:Name="CbOpponentDecks2026" Height="28" Margin="15,0,0,10" SelectionChanged="CbOpponentDecks2026_SelectionChanged"/>
                            
                            <RadioButton x:Name="RbOpponentOther" Content="Other Decks" GroupName="OpponentDeckGroup" Checked="RbOpponentCategory_Checked"/>
                            <ComboBox x:Name="CbOpponentDecksOther" Height="28" Margin="15,0,0,12" SelectionChanged="CbOpponentDecksOther_SelectionChanged"/>
                        </StackPanel>

                        <Button x:Name="BtnConnectAi" Content="Start &amp; Connect Bot to Room" Click="BtnConnectAi_Click" Margin="0,10,0,0" Height="36"/>
                        
                        <!-- Quick Tips Box (AI clean-up) -->
                        <Border BorderBrush="#CCCCCC" BorderThickness="1.5" CornerRadius="4" Padding="12" Margin="0,15,0,0" Background="White">
                            <StackPanel>
                                <TextBlock Text="Quick Tips" FontWeight="Bold" Foreground="#111111" Margin="0,0,0,6" FontSize="13"/>
                                <TextBlock Text="- Single Bot: Connect to EDOPro room" Foreground="#333333" Margin="0,0,0,4" TextWrapping="Wrap"/>
                                <TextBlock Text="- Two Bots: Auto-duel for training data" Foreground="#333333" Margin="0,0,0,4" TextWrapping="Wrap"/>
                                <TextBlock Text="- Rule-based logic matches card play rules strictly" Foreground="#333333" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>

            <!-- Console logs (Right Column - Optimized Height & Copying) -->
            <GroupBox Grid.Column="2" Header="Console Output Logs" Margin="0,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Fixed TextBox layout & formatting for high performance logs copying -->
                    <TextBox x:Name="TxtConsole" Grid.Row="0" IsReadOnly="True" 
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                             Background="White" Foreground="Black" FontFamily="Consolas" FontSize="11.5" 
                             Padding="8" BorderBrush="#222222" BorderThickness="1.5" 
                             AcceptsReturn="True" TextWrapping="NoWrap"/>
                    
                    <Grid Grid.Row="1" Margin="0,10,0,0">
                        <TextBlock x:Name="TxtConsoleStatus" Text="Ready." VerticalAlignment="Center" Foreground="#111111" FontWeight="SemiBold"/>
                        <Button x:Name="BtnClearConsole" Content="Clear Logs" HorizontalAlignment="Right" Click="BtnClearConsole_Click" Padding="12,4"/>
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Footer (AI clean-up) -->
        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="0,1.5,0,0" Padding="5,10,5,0" Margin="0,12,0,0">
            <Grid>
                <TextBlock Text="YGO Platform v2.3 - Standalone Bot Runner Launcher (dashbot)" FontSize="11" Foreground="#666666" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Text="Powered by .NET 10" FontSize="11" Foreground="#666666" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </Grid>
        </Border>
    </Grid>
</Window>
